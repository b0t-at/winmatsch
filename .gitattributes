# Normalize all text files to LF in the repository and working tree.
# This keeps dotnet-format results and YAML round-trip fixtures identical everywhere.
* text=auto eol=lf

# Binary files
*.png binary
*.jpg binary
*.ico binary
*.gif binary
*.zip binary
*.msi binary
*.msix binary
*.exe binary
*.dll binary
